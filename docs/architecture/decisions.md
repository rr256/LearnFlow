---
title: LearnFlow Architecture Decision Register
status: approved
owner: architecture
last_updated: 2026-08-18
related:
  - ../00-project-context.md
  - ../adr/README.md
  - ../adr/ADR-000-template.md
  - ../adr/ADR-026-monthly-study-view.md
  - ../adr/ADR-028-revision-workflow.md
  - ../adr/ADR-027-plan-feasibility.md
  - ../adr/ADR-029-progress-overview.md
  - ../adr/ADR-030-learning-stages-by-subject-panel.md
  - ../adr/ADR-031-priority-focus-panel.md
  - ../adr/ADR-032-learning-resource-catalogue.md
  - ../adr/ADR-033-checkpoint-practice-workflow.md
---

# LearnFlow Architecture Decision Register

## Purpose

Provide a concise index of approved, durable LearnFlow decisions. This register is a navigation aid, not a replacement for detailed documents or Architecture Decision Records (ADRs).

When a decision is significant enough to affect multiple modules, future contributors, data, security, or deployment, create the linked ADR before implementing the affected area.

## Decision Statuses

- **Approved:** agreed direction; implementation may proceed when its detailed documentation is ready.
- **Accepted — ADR-NNN:** approved direction with an accepted ADR recording its rationale.
- **ADR pending:** approved direction that still requires a formal ADR before or alongside implementation.
- **ADR proposed — ADR-NNN:** approved direction whose ADR is drafted and awaiting project-owner acceptance.
- **Deferred:** intentionally not implemented now; revisit only when the stated trigger occurs.

These are register statuses. They are distinct from the document `status` field in front matter, which uses the vocabularies defined in [documentation standards](../development/documentation-standards.md).

## Approved Decisions

| ID | Decision | Status | Detail | ADR status |
| --- | --- | --- | --- | --- |
| DEC-001 | LearnFlow is a generic AI learning-mentor platform; GATE CSE is the first curated learning program. | Approved | [Vision](../vision/vision.md), [Domain model](../domain/domain-model.md) | Product decision; ADR optional |
| DEC-002 | The first release is a local-first, single-learner MVP. | Approved | [MVP scope](../requirements/mvp.md), [Non-functional requirements](../requirements/non-functional.md) | ADR pending if deployment boundary is formalized |
| DEC-003 | Curriculum, subjects, topics, and subtopics are data-driven; they are not hardcoded in frontend code. | Approved | [Functional requirements](../requirements/functional.md), [Domain model](../domain/domain-model.md) | Accepted — [ADR-012](../adr/ADR-012-curriculum-seed-and-reconciliation.md) |
| DEC-004 | The initial curriculum is a verified GATE CSE curriculum, transcribed from the official syllabus and traceable to it. Syllabus-PDF extraction is a future reviewed workflow, not an MVP feature. | Approved | [MVP scope](../requirements/mvp.md), [Domain model](../domain/domain-model.md), [Database migrations](../database/migrations.md) | Accepted — [ADR-012](../adr/ADR-012-curriculum-seed-and-reconciliation.md) |
| DEC-005 | LearnFlow uses Clean Architecture with domain, application, presentation, infrastructure, and composition-root boundaries. | Approved | [Clean Architecture](clean-architecture.md) | Accepted — [ADR-001](../adr/ADR-001-clean-architecture.md) |
| DEC-006 | External capabilities use provider interfaces/adapters where realistic replacement is expected. | Approved | [Provider pattern](provider-pattern.md) | Accepted — [ADR-002](../adr/ADR-002-provider-pattern.md) |
| DEC-007 | Python with FastAPI is the initial backend/API technology. | Approved | [Architecture overview](overview.md) | ADR pending |
| DEC-008 | Next.js with TypeScript is the initial web frontend technology. Learner-facing pages reach the API from the Next.js server, so the browser makes no backend call and the API needs no CORS middleware in this scope. CSS Modules, Vitest with React Testing Library, ESLint with `eslint-config-next`, and npm with a committed lockfile are the styling, test, lint, and package-manager choices. | Approved | [Architecture overview](overview.md), [Technology stack](../development/tech-stack.md), [Repository and folder structure](../development/folder-structure.md) | Accepted — [ADR-015](../adr/ADR-015-frontend-foundation-and-server-rendered-api-access.md) |
| DEC-009 | PostgreSQL stores structured transactional data; repositories isolate persistence implementation. | Approved | [Architecture overview](overview.md), [Provider pattern](provider-pattern.md) | Accepted — [ADR-003](../adr/ADR-003-postgresql-persistence.md) |
| DEC-010 | Local filesystem storage is the initial source-file storage implementation; cloud storage is a future adapter. | Approved | [Provider pattern](provider-pattern.md) | ADR pending |
| DEC-011 | Ollama is the initial local AI provider for both generation and embeddings; AI is used for reasoning/generation, not durable memory or direct state changes. | Approved | [Architecture overview](overview.md), [Provider pattern](provider-pattern.md) | Accepted — [ADR-004](../adr/ADR-004-ollama-local-ai-provider.md) |
| DEC-012 | ChromaDB is the initial vector-search implementation; embeddings and retrieval remain replaceable. | Approved | [Architecture overview](overview.md), [Provider pattern](provider-pattern.md) | ADR pending |
| DEC-013 | Docker Compose provides reproducible local development; Ollama initially runs on the host machine and is configured by endpoint. | Approved | [Architecture overview](overview.md) | Accepted — [ADR-005](../adr/ADR-005-docker-compose-local-development.md) |
| DEC-014 | Start with a predictable custom orchestrator for product learning responsibilities; evaluate an agent framework only when workflows justify it. | Approved | [Architecture overview](overview.md), [MVP scope](../requirements/mvp.md) | Accepted — [ADR-006](../adr/ADR-006-custom-agent-orchestration.md) |
| DEC-015 | Learner progress is evidence-based: material completion, learning stage, activities, quizzes, external tests, mistakes, and revisions remain separate signals. | Approved | [Functional requirements](../requirements/functional.md), [Domain model](../domain/domain-model.md) | ADR pending |
| DEC-016 | External test-series results are manually entered by the learner; there is no third-party platform scraping, login sharing, or direct integration in the MVP. | Approved | [Functional requirements](../requirements/functional.md) | ADR pending |
| DEC-017 | Documentation is a source of truth: major decisions require ADRs, and implementation changes update affected documents. | Approved | [Project context](../00-project-context.md), [Documentation standards](../development/documentation-standards.md) | Accepted — [ADR-007](../adr/ADR-007-documentation-and-adr-policy.md) |
| DEC-018 | A mistake record has exactly one discovery source — quiz-attempt answer, external test result, revision record, or study activity — modelled as named nullable foreign keys rather than a polymorphic source field. | Approved | [Domain model](../domain/domain-model.md), [Database schema](../database/schema.md) | Accepted — [ADR-008](../adr/ADR-008-assessment-and-mistake-evidence-model.md) |
| DEC-019 | A checkpoint quiz covers one or more topics through a `checkpoint_quiz_topics` join table; the application requires at least one linked topic. | Approved | [Domain entities](../domain/entities.md), [Database schema](../database/schema.md) | Accepted — [ADR-008](../adr/ADR-008-assessment-and-mistake-evidence-model.md) |
| DEC-020 | Topic performance evidence belongs only to an external test result; checkpoint quiz outcomes reach topic progress through quiz attempts and question topic links. | Approved | [Domain model](../domain/domain-model.md), [Domain entities](../domain/entities.md) | Accepted — [ADR-008](../adr/ADR-008-assessment-and-mistake-evidence-model.md) |
| DEC-021 | Monthly plans are an MVP planning type alongside roadmap, weekly, and daily plans. | Approved | [MVP scope](../requirements/mvp.md), [Functional requirements](../requirements/functional.md), [Database schema](../database/schema.md) | Product decision; ADR optional |
| DEC-022 | Configuration variables use three categories — core runtime (`APP_*`, `API_*`), capability (`<CAPABILITY>_PROVIDER` plus capability-level settings), and vendor (`<VENDOR>_<SETTING>`). `EMBEDDING_MODEL` is removed; `API_BASE_URL` is frontend configuration. `deployment/environments.md` is the authoritative catalogue, and configuration is validated before the application is created. | Approved | [Environments and configuration](../deployment/environments.md) | Accepted — [ADR-009](../adr/ADR-009-configuration-naming-and-validation.md) |
| DEC-023 | Changes reach `main` through a pull request. CI runs the checks enumerated in [CI/CD strategy](../deployment/ci-cd.md) on pull requests to `main` and pushes to `main`; each remaining check is added with the artifact it verifies. AI-assisted delivery follows one repeatable workflow that stops at an open pull request and never merges. | Approved | [CI/CD strategy](../deployment/ci-cd.md), [Git workflow](../development/git-workflow.md), [Engineering AI workflow](../ai/engineering-ai.md) | Accepted — [ADR-010](../adr/ADR-010-feature-delivery-workflow.md) |
| DEC-024 | PostgreSQL persistence uses synchronous SQLAlchemy with psycopg 3. The schema is migrated one area per milestone, starting with the curriculum tables; `schema.md` remains the approved target for every area it documents — six when this decision was recorded, seven since DEC-026 added the examination schedule. Controlled values are validated text guarded by a `CHECK` constraint, and one active curriculum version per program is enforced by a partial unique index. | Approved | [Database schema](../database/schema.md), [Database migrations](../database/migrations.md) | Accepted — [ADR-011](../adr/ADR-011-sqlalchemy-persistence-implementation.md) |
| DEC-025 | Curriculum is loaded from a versioned data file by an idempotent seed that matches every record on a database-enforced natural key, updates in place, and never deletes. A topic code is unique within its subject; a subject or topic dropped from the source keeps its row. | Approved | [Database migrations](../database/migrations.md), [Database schema](../database/schema.md) | Accepted — [ADR-012](../adr/ADR-012-curriculum-seed-and-reconciliation.md) |
| DEC-026 | An examination period is published reference data modelled as a dated window, never a single guessed date. An examination schedule belongs to a learning program and cycle, carries its source and a `provisional`/`confirmed` status, and is loaded by its own idempotent seed. A study goal references a schedule, a target date, or both, enforced by a `CHECK`. The default learner timezone is `APP_DEFAULT_TIMEZONE`, defaulting to `Asia/Kolkata`. | Approved | [Database schema](../database/schema.md), [Domain model](../domain/domain-model.md), [Terminology](../domain/terminology.md) | Accepted — [ADR-013](../adr/ADR-013-examination-schedule-and-study-goal.md) |
| DEC-027 | Every `/api/v1` response uses the `data` envelope, and every collection carries a `pagination` block with `limit`, `offset`, and `total` from its first version. Every failure — including one the application did not raise — returns the error envelope with a code from a closed catalogue: `not_found`, `method_not_allowed`, `validation_error`, `internal_error`, and `request_failed` as the fallback. A `details` entry is `field`, `message`, and `type`, and never the rejected input; a `500` never carries the reason. `conflict` joined the catalogue for `409` with DEC-028. | Approved | [API conventions](../api/conventions.md), [API endpoints](../api/endpoints.md), [API versioning](../api/versioning.md) | Accepted — [ADR-014](../adr/ADR-014-api-response-contract.md) |
| DEC-028 | The learner and study-goal endpoint deferral is discharged for LRN-001, LRN-002, and GOAL-001 to GOAL-004, and EXM-001 is added so a learner can choose a published examination cycle. Their request and response contracts are fixed, `409` gains the code `conflict`, and the setup screen writes through a Next.js server action, so no browser calls the API and no CORS is introduced. GOAL-005 stayed deferred on the `day_of_week` decision, not on a client; **DEC-030 has since taken that decision and implemented it**. Per-endpoint fields and rules live in [API endpoints](../api/endpoints.md#learner-setup-and-goal-endpoints). | Approved | [API endpoints](../api/endpoints.md), [API conventions](../api/conventions.md), [Database schema](../database/schema.md) | Accepted — [ADR-016](../adr/ADR-016-learner-onboarding-api-contracts.md) |
| DEC-029 | Manual topic progress is a learner-owned learning stage, contracted by PRG-004 and PRG-002. The five approved stages are stored and sent as `snake_case`; the labels stay presentation. A topic with no record has no stage and reads as *Not explored*, so a read never writes and no record is created on a learner's behalf; there is no way to clear one. `learner_topic_progress` is created with five of its eight documented columns — `material_status`, `material_completed_at`, and `last_studied_at` wait for the code that writes them, while `stage_source` does not, because its later absence could not be backfilled. A grouping topic is refused in the application, CUR-003 gains no learner-owned field, and the write goes through a Next.js server action, so no browser calls the API. Per-endpoint fields and rules live in [API endpoints](../api/endpoints.md#progress-and-study-activity-endpoints). | Approved | [API endpoints](../api/endpoints.md), [Database schema](../database/schema.md), [Functional requirements](../requirements/functional.md) | Accepted — [ADR-017](../adr/ADR-017-topic-progress-api-and-schema.md) |
| DEC-030 | Weekly availability is one row per study goal and day of the week, holding minutes rather than clock times. `day_of_week` is the `snake_case` day *name* in the database and on the wire, which **retires** the numbering convention DEC-024 left open rather than answering it: Python, JavaScript, and PostgreSQL disagree about which day is zero, and a wrong guess misfiles a week with no error anywhere. GOAL-005 replaces a whole week at once — the days named become the learner's availability, a day left out is removed, an explicit empty list clears it, and saving an unchanged week writes nothing. A day of zero minutes is one deliberately kept free, distinct from a day with no row. Every goal response carries the saved week; nothing reports a total, because summing one is planning work. Per-endpoint fields and rules live in [API endpoints](../api/endpoints.md#goal-005-put-apiv1study-goalsgoal_idavailability). | Approved | [API endpoints](../api/endpoints.md), [Database schema](../database/schema.md), [Terminology](../domain/terminology.md) | Accepted — [ADR-018](../adr/ADR-018-weekly-availability-slots.md) |
| DEC-031 | A learner's planning preferences are a preferred session length and a topic order, held as two typed nullable columns on `study_goals` rather than the `planning_preferences jsonb` DEC-009's schema first approved — no `CHECK` reaches a key inside JSON, and `topic_sequencing` is a controlled value. Both are nullable with no default, so a preference nobody set never reads as one somebody chose. GOAL-001 and GOAL-004 accept them and every goal response carries them, so **no endpoint was added**; a supplied group **replaces** the stored one, as GOAL-005 replaces a week, because the form shows every preference at once. A session length is a duration, not a time of day, so DEC-030's refusal of clock times stands. This completes FR-002's second acceptance criterion, whose other half DEC-030 delivered, and it creates columns **before** the planner that reads them — a deliberate departure from DEC-024's ordering rule, recorded with its risk. Per-endpoint fields and rules live in [API endpoints](../api/endpoints.md#learner-setup-and-goal-endpoints). | Approved | [API endpoints](../api/endpoints.md), [Database schema](../database/schema.md), [Terminology](../domain/terminology.md) | Accepted — [ADR-019](../adr/ADR-019-study-goal-planning-preferences.md) |
| DEC-032 | The initial study plan is generated deterministically as two plans per goal: a `roadmap` ordering every trackable topic across the goal's horizon, and a `weekly` plan placing the first of them onto the next seven days from the saved availability. No AI provider is involved, so the same inputs always produce the same plan. Topic order is the syllabus's own, or a topological order over `prerequisite` links with syllabus position as the tie-break; the curated GATE CSE curriculum stores no such link, and the plan says so rather than implying otherwise. A session is the learner's `preferred_session_minutes` or 60 minutes the planner chose and names as its own — nothing is stored against the goal. The horizon is the earlier of the examination window's first sitting day and the target date. A recorded learning stage explains an item and never reorders one. Generating again **supersedes** the goal's active plans rather than refusing or deleting. `study_plans` and `plan_items` complete the learner-planning area, with `plan_type`, `status`, and `action_type` as `varchar(32)` guarded by a `CHECK` rather than the documented `text`. PLN-001 to PLN-003 are implemented and PLN-004/PLN-005 are not — **DEC-033 has since implemented PLN-004, and DEC-034 PLN-005**; the planning rules are the first module in `backend/app/domain/`. This completes FR-002's last acceptance criterion. Per-endpoint fields and rules live in [API endpoints](../api/endpoints.md#planning-endpoints). | Approved | [API endpoints](../api/endpoints.md), [Database schema](../database/schema.md), [Domain model](../domain/domain-model.md) | Accepted — [ADR-020](../adr/ADR-020-initial-study-plan-generation.md) |
| DEC-033 | A learner can mark a plan item completed and return it to `planned`, over PLN-004 — `PATCH /api/v1/plan-items/{plan_item_id}`, the path catalogued since the documentation foundation. It accepts `completed` and `planned` and **refuses** `skipped` and `postponed`, which are values the column holds and PLN-005's re-planning would have to answer for: postponing work raises the question of what it moves *to*. Completing is **reversible**, as a learning stage is (DEC-029), and `completed_at` is written from the server's clock rather than accepted from a caller, so no request can backdate work. **Only the named item moves**: no plan, no other item — including a roadmap item naming the same topic, which nothing links but the topic — and no learning stage, because a plan item records whether planned work happened rather than that a topic is understood. Nothing is re-planned and **nothing is counted**, so no completion total joins the totals DEC-032 refused. An item on a **superseded** plan is refused with `409`, because such a plan is kept precisely to read as it was written. **No migration**: `plan_items.status` and `completed_at` were created by `20260806_03` ahead of this code, which is the ordering exception DEC-029 made for `stage_source` paying off. The write goes through a Next.js server action, so no browser calls the API. This is the first delivery against [FR-004](../requirements/functional.md#fr-004-plan-adaptation) and meets one third of its first acceptance criterion. Per-endpoint fields and rules live in [API endpoints](../api/endpoints.md#pln-004-patch-apiv1plan-itemsplan_item_id). | Approved | [API endpoints](../api/endpoints.md), [Database schema](../database/schema.md), [Domain model](../domain/domain-model.md) | Accepted — [ADR-021](../adr/ADR-021-plan-item-completion.md) |
| DEC-034 | A learner can **request** that their active study plan be rebuilt around what has happened, over PLN-005 — `POST /api/v1/study-goals/{study_goal_id}/adapt`, which **departs from the catalogued** `/study-plans/{plan_id}/adapt` because adaptation supersedes and rewrites every active plan of a goal. Nothing adapts on its own: completing an item re-plans nothing and saving a study week re-plans nothing, which keeps DEC-033's promise that only the named item moves. Adaptation **supersedes** as DEC-032's generation does. A topic with a completed session anywhere on the goal — including on a plan long superseded — is **not planned again**, and the exclusion is applied before the ordering and placement rules run, so an adapted plan is a real plan over what remains. Work whose day passed with the task undone is written **`postponed`** on the plan being set aside and re-placed on the new one, which answers the question DEC-033 could not: postponed *to where?* What counts as behind is a pure domain rule — today is not behind, an undated roadmap item is never behind, and completed work is never behind. `skipped` stays unwritten. Roadmap and weekly only; `monthly` and `daily` remain constrained and unwritten. **No migration**: every input was already stored, and `plan_items.status` has accepted `postponed` since `20260806_03`. The endpoint takes no request body and the write goes through a Next.js server action. This meets FR-004's second acceptance criterion and completes its first but for `skipped`; the third, trade-off visibility, remains unmet. Per-endpoint fields and rules live in [API endpoints](../api/endpoints.md#pln-005-post-apiv1study-goalsstudy_goal_idadapt). | Approved | [API endpoints](../api/endpoints.md), [Database schema](../database/schema.md), [Domain model](../domain/domain-model.md) | Accepted — [ADR-022](../adr/ADR-022-plan-adaptation.md) |
| DEC-035 | A learner can see **what to study today** on a screen of its own, `/plan/today`, built by **reading** the goal's active `weekly` plan filtered to one date. It is **not** a `daily` plan: that `plan_type` stays constrained and unwritten, as DEC-032 left it, and what a daily plan *contains* stays undecided rather than settled as a side effect of adding a screen. "Today" is the **learner's own** calendar date, resolved on the Next.js server from `learners.timezone` (DEC-026) with the same UTC fallback the backend applies — never the server's own zone, which would be wrong by a whole day on the screen most sensitive to it. Work the plan placed on days that have **passed** with the task undone is shown under its own heading, described as *items* rather than as the learner, and **nothing moves it**: no status is written and no adaptation is triggered, so DEC-034's "the learner asks; nothing adapts on its own" and DEC-033's "only the named item moves" both hold. The three overdue boundaries are **mirrored for display** from `select_overdue`, which stays the authoritative rule for what adaptation writes; the duplication is the recorded cost. **No new endpoint, no column, no table, no migration** — the screen consumes LRN-001, GOAL-002, PLN-002, PLN-003, and PLN-004 unchanged, through the same server-side client, so no browser calls the API. Nothing is counted, totalled, ranked, or scored. This delivers the **daily** half of [FR-003](../requirements/functional.md#fr-003-study-timeline-and-plan)'s second acceptance criterion; monthly remains unbuilt. *(The monthly sentence is overtaken by DEC-038, which delivers that half the same way — as a reading rather than a plan — so the criterion is now met in full. DEC-035 is otherwise unchanged, and no `daily` plan is written.)* | Approved | [API endpoints](../api/endpoints.md#planning-endpoints), [Terminology](../domain/terminology.md), [Repository and folder structure](../development/folder-structure.md) | Accepted — [ADR-023](../adr/ADR-023-daily-study-view.md) |
| DEC-036 | A learner can **skip** a plan item, over PLN-004 — the same catalogued `PATCH /api/v1/plan-items/{plan_item_id}`, which now accepts `planned`, `completed`, and `skipped` and still refuses `postponed`, because adaptation writes that as it sets a plan aside. **Every move between the three is allowed**, so nothing here is one-way, which is DEC-029's position on a learning stage and DEC-033's on completion. **Skipping settles the item, not the topic**: a skipped item is never *overdue*, so adaptation leaves it exactly as the learner left it rather than overwriting their statement with `postponed` — and its **topic is planned again**, unlike a completed one, which DEC-034 excludes from every plan that follows. That difference is deliberate and is what keeps a skip reversible in practice: a skip lives on the plan it was made on, and DEC-033's `409` for a superseded plan applies to it unchanged and without exception, so retiring the topic would have made a skip irreversible the moment the learner adapted. What counts as behind is now stated as *settled* rather than *done* — `DatedItem.is_done` becomes `is_settled`, and `select_overdue` gains a fourth boundary beside DEC-034's three. A skipped item **stays in place** on both `/plan` panels and in the DEC-035 daily view, marked in words, and is **left out of *From earlier days***, whose display partition mirrors the same rule. **No migration, no column, no table, and no endpoint**: `plan_items.status` has accepted `skipped` since `20260806_03`, and this is the last of that constraint's four values to be written. There is deliberately **no `skipped_at`** and no reason field, and **nothing counts skips**. The write goes through the existing Next.js server action, so no browser calls the API. Of [FR-004](../requirements/functional.md#fr-004-plan-adaptation)'s **first** acceptance criterion, a learner can now explicitly mark an item **completed** or **skipped**; they still **cannot** mark one **postponed**, which adaptation writes for items whose day passed with the work still `planned`, so the criterion is **not met in full**. The third, trade-off visibility, is not built at all. *(The postponement sentence is overtaken by DEC-037: a learner can now mark an item `postponed`, PLN-004 accepts all four statuses, and FR-004's first criterion is met in full. Everything else here stands.)* Per-endpoint fields and rules live in [API endpoints](../api/endpoints.md#pln-004-patch-apiv1plan-itemsplan_item_id). | Approved | [API endpoints](../api/endpoints.md), [Database schema](../database/schema.md), [Terminology](../domain/terminology.md) | Accepted — [ADR-024](../adr/ADR-024-plan-item-skipping.md) |
| DEC-037 | A learner can **postpone** a plan item, over PLN-004 — the same catalogued `PATCH /api/v1/plan-items/{plan_item_id}`, which now accepts **all four** values `plan_items.status` holds, so what a learner may *ask for* and what the column may *hold* coincide. **Every move between the four is allowed**, so nothing here is one-way, which is DEC-029's position on a learning stage and DEC-033's on completion. This **completes [FR-004](../requirements/functional.md#fr-004-plan-adaptation)'s first acceptance criterion in full**: all three of its verbs are now learner actions. **`postponed` gains a second writer and keeps one meaning** — the learner saying the work is not happening *yet*, and DEC-034's adaptation marking work whose day passed with nothing said about it — and **postponing moves nothing on its own**: it takes no date, rewrites no `scheduled_for`, and triggers no adaptation, so the work is placed again only when the learner asks. **A postponed item is settled**: `POSTPONED` joins `SETTLED_STATUSES`, so it is never *overdue*, adaptation leaves it exactly as the learner marked it, and `postponed_plan_item_ids` continues to name only what adaptation itself set aside. Its **topic is planned again**, as a skipped one's is under DEC-036 and unlike a completed one's under DEC-034 — so the next plan treats a skip and a postponement identically, and the difference between them is what the record says. The domain rule is untouched: DEC-036's `is_settled` rename absorbed this, and `select_overdue` gains a fifth clause rather than a boundary. An **undated roadmap item may be postponed**, because the endpoint knows nothing about dated versus undated items. A postponed item **stays in place** on both `/plan` panels and in the DEC-035 daily view, marked in words, and is **left out of *From earlier days***, whose display partition mirrors the same set — now named on both sides. **No migration, no column, no table, and no endpoint**: `plan_items.status` has accepted `postponed` since `20260806_03` and DEC-034 has written it since. There is deliberately **no `postponed_at`**, no reason field, and **no column recording which writer set the status**, so a superseded plan cannot distinguish the learner's postponement from adaptation's. **Nothing counts postponements.** The write goes through the existing Next.js server action, so no browser calls the API. FR-004's third criterion, trade-off visibility, is still not built at all. Per-endpoint fields and rules live in [API endpoints](../api/endpoints.md#pln-004-patch-apiv1plan-itemsplan_item_id). | Approved | [API endpoints](../api/endpoints.md), [Database schema](../database/schema.md), [Terminology](../domain/terminology.md) | Accepted — [ADR-025](../adr/ADR-025-learner-postponement.md) |
| DEC-038 | A learner can see **their month** on a screen of its own, `/plan/month`, built by **reading** the goal's active `roadmap` and `weekly` plans grouped to one calendar month. It is **not** a `monthly` plan: that `plan_type` stays constrained and unwritten, as DEC-032 left it and DEC-035 left `daily`, and what a monthly plan *contains* stays undecided rather than settled as a side effect of adding a screen. This answers the question DEC-035 left open by name — whether a monthly view is a reading or a plan — and **completes [FR-003](../requirements/functional.md#fr-003-study-timeline-and-plan)'s second acceptance criterion in full**, monthly having been its last unbuilt half. **The frontend does not decide how much of a roadmap a month holds.** A weekly plan dates seven days, so a month holds at most one week of dated work; the screen shows those days and then the roadmap topics the week has not dated, **openly undated**, rather than spreading them across the month's remaining days. Projecting the saved study week and placing work onto it was rejected: placing sessions is planning arithmetic the backend owns, and dates existing in no stored record would disagree with the plan at the first adaptation. The month is the **learner's own**, from `learners.timezone` with the same UTC fallback, and `learnerMonth` is derived from DEC-035's `learnerToday` so one conversion serves both. **Only the current month is reachable**; browsing to another was rejected while nothing is dated beyond the week. A **settled** item keeps its place on its day, marked in words, as it does on every other panel. **The screen is read-only** — no status control, no generate control, no adapt control — which **departs from DEC-035**, whose daily view reuses `PlanItemStatusControl` so that an item behaves identically on every screen; marking stays where a learner works, and the departure's cost is recorded. **No new endpoint, no column, no table, no migration**, and **no backend file changes at all** — the screen consumes LRN-001, GOAL-002, PLN-002, and PLN-003 unchanged, through the same server-side client, so no browser calls the API. Nothing is counted, totalled, ranked, or scored, and `select_overdue` is neither read nor mirrored, because this screen makes no claim about what is overdue. FR-004's third criterion, trade-off visibility, is still not built at all. | Approved | [API endpoints](../api/endpoints.md#planning-endpoints), [Terminology](../domain/terminology.md), [Repository and folder structure](../development/folder-structure.md) | Accepted — [ADR-026](../adr/ADR-026-monthly-study-view.md) |
| DEC-039 | A learner can see **whether the study time they saved covers the work left before their horizon**, over PLN-006 — `GET /api/v1/study-goals/{study_goal_id}/plan-feasibility`, goal-scoped as DEC-034's adaptation is because the horizon, the week, and the preferences all belong to the goal. This meets **[FR-004](../requirements/functional.md#fr-004-plan-adaptation)'s third acceptance criterion**, the last one unmet, which DEC-032, DEC-034, DEC-036, DEC-037, and DEC-038 each recorded as an open gap. **The arithmetic is a pure domain rule**, `assess_horizon_coverage`, the fourth in `backend/app/domain/study_planning.py`: one session for each remaining topic against the minutes the saved week offers on every day from today to the horizon, both ends included, counted by weekday rather than walked day by day. It is a **backend** rule rather than a screen calculation, which is where this **departs from DEC-038's frontend-only reading** — [terminology](../domain/terminology.md) states that totalling available time is planning arithmetic **the planner** should perform, and [coding standards](../development/coding-standards.md#ui-responsibilities) reserve planning for the backend. **It is a live read and writes nothing**: no plan, no availability, no preference, and no item status moves, and nothing adapts, so a learner may ask as often as they like and the answer moves when their week does. Writing the sentence into `generation_reason` was rejected because DEC-032 fixes that a plan's reasons are never rewritten, so it would go stale the moment the learner edited the input. **Three verdicts**: `sufficient`, `insufficient`, and `unknown` — the third an answer rather than a failure, with `unknown_reason` distinguishing a goal aiming at no date from one with no saved week, because the two ask the learner for different things. **A week saved and deliberately kept free is neither**: that is zero minutes, a real answer, preserving DEC-030's distinction. An unset session length is named as the **planner's own choice**, never a default (DEC-031). **Only a completed topic is excluded**, exactly as DEC-034's adaptation excludes it; a skipped or postponed topic still needs time because the next plan places it again. **Everything is reported as counts and durations — never a ratio, a percentage, or a proportion**, and a shortfall is never a negative surplus. The result appears on `/plan` above the week, with no control of any kind. **No column, no table, no migration**, and no learner flow changed. | Approved | [API endpoints](../api/endpoints.md#planning-endpoints), [Terminology](../domain/terminology.md), [Domain model](../domain/domain-model.md) | Accepted — [ADR-027](../adr/ADR-027-plan-feasibility.md) |
| DEC-040 | A learner can **schedule revisions** for topics they have finished, see what is ready to review, and record what became of each one — over REV-001 to REV-003 as catalogued, plus **REV-004 `POST /api/v1/revisions/schedule`**, which the catalogue did not hold because nothing there creates a record. **The learner asks; nothing schedules on its own**: completing a plan item creates no revision, and neither does completing a revision, so a list never grows under a learner working through it. Asking twice creates nothing the second time. A topic returns an **interval after the work it follows**, and the interval comes from the **learning stage the learner recorded** — 7 days with none recorded or at `building_foundation`, rising to 21 at `strong_understanding`. **These are LearnFlow's intervals, named as its own** wherever they appear; a longer wait is not a better mark, and nothing ranks or scores. This does not disturb DEC-032's refusal to let a stage reorder a **plan**: FR-006 names the stage as a revision input, which is a different question about a different record. **A completed review schedules the next**, which is FR-006's *prior revision history*; a **skipped or postponed** review is left alone, because the learner has answered and every status is reversible. **A revision is not a plan item.** `plan_items.action_type = 'revise'` stays unwritten and no revision enters a plan, because DEC-034's adaptation supersedes every active plan of a goal and a review the learner acted on must survive that. **Adaptation is untouched** and reads no revision. **Nothing writes a learning stage** — rule 4 again: a review records that the review happened, not that the topic is understood. REV-003 mirrors PLN-004 exactly: `due`, `completed`, `skipped`, `postponed`, every move allowed and reversible, `completed_at` from the server clock, **no `skipped_at`, no date, and no reason field**. `scheduled` and `scheduled_for` are created and **unwritten**, as DEC-032's `plan_items.status` values were. It lives at **`/revisions`**, its own route; the roadmap, week, day, and month views are unchanged. **This needs the first migration since `20260806_03`** — `20260813_01`, one `CREATE TABLE` and one index, altering nothing — and `revision_records` carries **`recommendation_reason`, a column the approved table does not list**: a due date is computed from the stage recorded at that moment, so a reason recomputed later from a changed stage would contradict the stored date. Freezing it keeps the record self-explaining, as DEC-032 requires of a plan. Nothing is counted, ranked, or scored. This delivers the **built** part of [FR-006](../requirements/functional.md#fr-006-revision-guidance) — revision scheduling, status updates, and a view of what is due — and is the last of Milestone 3's requirements to be started. **FR-006 is not met in full.** Two of its four criteria are met (seeing what is due; recording a completion or skip); the fourth is met on three of its four inputs, because no quiz or test evidence is stored; and the resource-and-practice half of the second is **deferred** to FR-007 and FR-009, which do not exist. | Approved | [API endpoints](../api/endpoints.md#revision-endpoints), [Database schema](../database/schema.md#revision_records), [Terminology](../domain/terminology.md) | Accepted — [ADR-028](../adr/ADR-028-revision-workflow.md) |
| DEC-041 | A learner can see **where their study stands** on a screen of its own, `/progress`, built by **reading** six existing contracts — LRN-001, GOAL-002, PLN-002, PLN-003, PLN-006, and REV-001. This is the **progress overview** [FR-011](../requirements/functional.md#fr-011-progress-overview) describes and the screen [terminology](../domain/terminology.md) has reserved the word *dashboard* for; the canonical name is **progress overview**, by the rule that a screen is named for what a learner does there rather than for a UI genre, and the heading a learner reads is *Where your study stands*. **PRG-001 is not built.** It fails the test DEC-035 set for a new endpoint — every fact is already a field of an existing response — and its catalogued purpose also promises *priority focus areas*, which need evidence nothing stores and would rank topics against each other, which terminology refuses. Fixing its shape now is a public contract the missing half would later break. **The screen counts nothing of its own.** The only figures on it are ones the API reported: a plan's `item_count`, and the counts and durations PLN-006 returned. Terminology forbids the three a dashboard reaches for first, by name — "nothing counts skips", "nothing counts postponements", and "no revision count appears in the interface at all" — so what a learner has marked is **listed** under the words for each status, with the topic, the plan the mark was made on, its day, and the plan's own reason, rather than tallied. No percentage, ratio, rate, streak, progress bar, `<progress>`, or `<meter>`, and `item_count` is stated on its own and never against a second number. **It writes nothing at all** — no status, generate, adapt, or scheduling control, and no `<button>` and no `<form>` — the read-only shape DEC-038 fixed for the monthly view, applied a second time and for the reason it gave: each panel names where its action lives and links to it, so one place per action survives. The date is the **learner's own**, from `learners.timezone` through the same `learnerToday` DEC-035 introduced, and today's work comes from the same `selectDailyWork`, so **no new timezone conversion and no new mirror of `select_overdue` is written** — `learnerToday` gains a third caller, and the frontend keeps the single settled set it already had. Work whose day has passed is **reported, not moved and not counted**. **Only the goal's active plans are read**, because DEC-033's `409` means a learner cannot write into a superseded one. **The home screen does not move**: terminology fixes `/` as *Your study setup* and states it "is not a *dashboard*", and overturning that is a decision about the landing screen rather than a side effect of adding this one. **No endpoint, no column, no table, no migration, and no backend file changes at all.** **FR-011 is not met in full**: of its four acceptance criteria, only the second — upcoming study tasks and revisions due — is met. Progress by subject and topic was deliberately left out of this change — **DEC-042 has since met that criterion**, so two of the four are now met; priority focus areas are not buildable, because no evidence is stored and ranking is refused *(overtaken by DEC-043, which gathers a priority focus from stored dates and statuses without ranking anything; the quiz, test, and mistake half stands)*; and quiz history and external test results need FR-009 and FR-010, which do not exist. | Approved | [API endpoints](../api/endpoints.md), [Terminology](../domain/terminology.md), [Repository and folder structure](../development/folder-structure.md) | Accepted — [ADR-029](../adr/ADR-029-progress-overview.md) |
| DEC-042 | The **learning stages a learner recorded are gathered by subject** on the progress overview, as a second **reading**: `/progress` joins PRG-002 to CUR-003 in the client — the join DEC-029 introduced for the curriculum view, read the other way round — and now makes eight reads rather than six. This meets [FR-011](../requirements/functional.md#fr-011-progress-overview)'s **first** acceptance criterion, for the progress LearnFlow stores, which is the *learning stage* alone: `material_status` is not created and `study_activities` does not exist. **PRG-001 stays unimplemented**, for the reasons DEC-041 gave, and **PRG-002 gains no `subject_id` filter and no subject name** — a filter narrows a collection this screen wants in full, and a subject name would put curriculum reference data inside a learner-owned response. **Only topics the learner recorded something against appear**, and a subject holding none is left out entirely rather than shown empty. **Nothing is counted**: no count beside a subject, no percentage of a subject recorded, no fraction, rate, streak, bar, `<progress>`, or `<meter>` — a figure beside a subject name passes none of terminology's three tests, since its subject is the learner. **Nothing is ranked**: the five stages are never ordered, grouped, or coloured by, because DEC-029 fixed that a learner may move to any stage from any stage and no code treats their order as a ranking. Subjects and topics keep the **curriculum's own order**, arrived at by walking the CUR-003 tree and picking up a record where one exists rather than by sorting PRG-002's newest-first list. **It writes nothing**: no `<button>`, `<form>`, `<select>`, or `<input>` — recording a stage stays beside the topic in the curriculum view, which the panel links to. An unreadable PRG-002 or CUR-003 empties **that panel alone**, said apart from "you have recorded nothing", and a record whose topic a re-seed dropped is kept under a heading naming that situation. **No endpoint, no column, no table, no migration, and no backend file changes at all.** **FR-011 is still not met in full**: priority focus areas and quiz/external-test history remain unmet, for the reasons DEC-041 records. *(The priority-focus half is overtaken by DEC-043, which meets that criterion partly.)* | Approved | [API endpoints](../api/endpoints.md), [Terminology](../domain/terminology.md), [Repository and folder structure](../development/folder-structure.md) | Accepted — [ADR-030](../adr/ADR-030-learning-stages-by-subject-panel.md) |
| DEC-043 | **Priority focus is gathered on the progress overview from three facts backend rules already decided**, as a third **reading**: work whose day has passed with nothing said about it (`select_overdue`, through the existing `selectDailyWork`), a review the backend reports `is_due` (REV-001), and a saved week PLN-006 says does not reach the horizon or cannot be assessed. `/progress` still reads the same **eight contracts** over **nine requests** and gains no endpoint. This **partly meets** [FR-011](../requirements/functional.md#fr-011-progress-overview)'s **third** acceptance criterion — for the evidence LearnFlow stores — and **supersedes one row of DEC-041 and DEC-042**, which recorded the criterion as "not buildable": that conclusion rested on a *ranking* design and on quiz, test, and mistake evidence, and this design ranks nothing while drawing only on dates, statuses, and availability that are stored. Everything else in both stands. **Nothing is ranked**: the three groups sit in a fixed presentation order that ranks nothing and says so, no entry is numbered, no group is styled as more urgent, and there is no top-anything or cap — capping would require choosing which to show. **The recorded learning stage is deliberately not a signal**, because selecting some of the five as priorities ranks them against each other, which DEC-029 and DEC-042 both refuse; where a stage explains an item it is already inside that item's frozen `recommendation_reason`. **Priorities are named items and reviews, never subjects**, since a subject-level claim needs either a per-subject count or a comparison against neighbouring subjects. Each entry carries a neutral fact naming the stored record and then **the sentence the backend wrote**, rendered unchanged. **It writes nothing**: no `<button>`, `<form>`, `<select>`, or `<input>` — each group names where its action lives and links to it. **Nothing is counted**: no tally of what needs attention, no percentage, fraction, streak, or bar. A feasibility reading that could not be taken contributes **no entry**, rather than a claim in either direction. **No endpoint, no column, no table, no migration, and no backend file changes at all.** **FR-011 is still not met in full**: quiz and external-test history remain unmet, and the third criterion is partly rather than wholly met, because quiz, test, and mistake evidence is stored nowhere. **PRG-001 stays unimplemented**, now waiting on that evidence alone. | Approved | [API endpoints](../api/endpoints.md), [Terminology](../domain/terminology.md), [Repository and folder structure](../development/folder-structure.md) | Accepted — [ADR-031](../adr/ADR-031-priority-focus-panel.md) |
| DEC-044 | A learner **catalogues their own study material and links it to curriculum topics**, which begins [FR-007](../requirements/functional.md#fr-007-learning-resource-organization) and creates the first two tables of the *Resources and RAG metadata* area — `resources` and `resource_topic_links`, in migration `20260816_01`. **A resource is metadata, never the material**: nothing is uploaded, downloaded, extracted, embedded, or indexed, and `storage_key`, `metadata`, and `resource_ingestions` are all absent, each waiting for the code that would maintain it, as DEC-024 requires and DEC-029 did for `learner_topic_progress`. **Nothing about the learner's own machine is stored**: `external_reference` accepts an `http` or `https` address alone, because [endpoints.md](../api/endpoints.md) has required since Milestone 0 that a resource endpoint never return an absolute local filesystem path — so the path is refused on the way in rather than filtered on the way out. Material that is not on the web is carried by `source_label`, in the learner's own words, and a resource must name at least one of the two, which is the approved *at least one of `storage_key` or `external_reference`* constraint read for a catalogue that stores no files. **FR-007's "references/paths to local video resources" is therefore only partly met**, and the local-file half arrives with the storage and ingestion change. **Nothing curated ships**: the catalogue starts empty, there is no seed script and no data file, and no external content enters the repository — `owner_learner_id` stays nullable so curated content needs no migration later, and nothing writes an ownerless row today. **Nothing is deleted**: RES-005 stays unimplemented and a learner puts material aside with `status: archived`, reversibly, which is DEC-034's keep-rather-than-delete position and DEC-036's reversibility. **Nothing is recommended, ranked, or counted**: a topic's material is what the learner linked to it, in the API's order; no figure appears beside a subject, a topic, or a review; and only `primary` of the four link roles is written, because grading how central a book is to a topic is a judgement nothing reads. **A resource may cover any stored topic, including a grouping heading**, which is deliberately unlike PRG-004 — a stage claims something about understanding a unit of work and a heading is not one, while a textbook may cover a whole subject. **Writing lives on `/resources` alone**; the curriculum view and `/revisions` show a topic's material read-only and name where the action lives, which is DEC-038's and DEC-041's shape. RES-001 to RES-004 are implemented at the **paths and methods catalogued**, adding and moving none; two of the catalogue's intent lines are narrower than they read, because nothing uploads a file and no subject-level link is stored, and endpoints.md records both. RES-005 to RES-008 are not implemented. This also meets the **resource half** of [FR-006](../requirements/functional.md#fr-006-revision-guidance)'s second criterion, which DEC-040 deferred; the practice half still waits on FR-009, so **FR-006 is still not met in full**. | Approved | [API endpoints](../api/endpoints.md), [Database schema](../database/schema.md), [Terminology](../domain/terminology.md) | Accepted — [ADR-032](../adr/ADR-032-learning-resource-catalogue.md) |
| DEC-045 | A learner can **write their own practice questions, take a checkpoint quiz built from them, and read an honest per-question result**, which begins [FR-009](../requirements/functional.md#fr-009-topic-checkpoint-practice) and creates the whole *Assessment* schema area in migration `20260818_01` — seven tables, altering nothing. **The learner writes every question and nothing curated ships**: no seed, no data file, no bundled previous-year paper, and no external fetch, which is DEC-044's position applied to question content and keeps LearnFlow clear of third-party licensing. `source_type` is always `curated`; `generated` waits on an AI provider and `verified_pyq` on a licensing position nobody has taken, and both stay in the `CHECK` unwritten. `questions.author_learner_id` is **added** beyond the approved shape, nullable, mirroring `resources.owner_learner_id`, because the schema's *Conventions* require a learner identifier on learner-owned records. **A question is never edited** — QZ-010 changes `status` alone — because `quiz_attempt_answers` references a question by identifier and rewriting a prompt would silently rewrite the history of every attempt marked against it; a learner corrects one by retiring it and writing another, which is DEC-034's keep-rather-than-overwrite position and a deliberate **departure from DEC-044**, whose resources may be edited because no result depends on their wording. `retired` is reversible and **nothing is deleted**; a quiz already assembled goes on asking a retired question. **A quiz asks every ready question for the chosen topics, in the order they were written** — deterministic with no AI provider, which is DEC-032's promise kept for an assessment. LearnFlow selects none and leaves none out, because choosing which few to ask is a ranking; there is no sampling, no shuffling, and no cap, so a quiz's length is the learner's own decision. A quiz naming no topic is refused, which is ADR-008's rule, and so is one for topics with no ready question. **The result states per-question outcomes and no total at all.** This resolves the standing conflict between [terminology](../domain/terminology.md), which forbids a score by name, and [schema.md](../database/schema.md), which approves `quiz_attempts.score` — **in terminology's favour**. `score`, `max_score`, `max_marks`, and `awarded_marks` are **not created**, so the one detail schema.md still records as undecided, numeric precision for score and marks columns, **stays undecided** rather than being settled by a change that would never read the answer. FR-009's "objective answers can be scored automatically" **is** met: every answer is marked correct or not correct, deterministically, by a pure rule in `backend/app/domain/checkpoint_marking.py` — the **third** module of the domain layer. **An unanswered question is not a wrong one**: `submitted_answer` and `is_correct` are both null, and the result says so in words; no option is pre-selected, because making a learner guess to satisfy a form is not practice. Only `multiple_choice` is written of the four documented forms, and option keys are assigned by position rather than accepted from a caller. **Nothing else moves**: no learning stage, no plan, no plan item, and no revision, which is FR-005's and FR-009's shared rule; a quiz is **not a plan item**, so `action_type = 'practise'` stays unwritten and PLN-005 is untouched, exactly as DEC-040 held for a revision. QZ-001, QZ-002, QZ-003, QZ-005, QZ-006, and QZ-007 are implemented as catalogued; **QZ-004 is not**, because saving one answer at a time needs a client this build has no reason to have, so **QZ-005 accepts the answers in its request body** — the one departure, of the kind DEC-034 recorded for `/adapt`. **QZ-008 to QZ-010 are new** at `/api/v1/practice-questions`, because the catalogue had no endpoint for creating a question at all. Starting an attempt is **safe to ask for twice** — an unfinished attempt is returned with `200` rather than a second created, which is DEC-040's position for a review already waiting — and submitting twice is refused with `409`. Writing lives on `/practice` alone, with `/practice/quizzes/{id}` to answer and `/practice/attempts/{id}` to read a result **read-only**; the curriculum, plan, month, day, revision, resource, and progress screens are **unchanged**, and `/progress` gains nothing because PRG-001 still waits on stored mistake evidence. **FR-009 is not met in full**: its stored-mistakes criterion needs `mistake_evidence`, whose four discovery sources reference two tables that do not exist, and its notes/PYQ-context criterion needs retrieval. **FR-006 is still not met in full** either: its practice half is deliberately left open, because surfacing a quiz on `/revisions` would mean recommending one. | Approved | [API endpoints](../api/endpoints.md), [Database schema](../database/schema.md), [Terminology](../domain/terminology.md), [Domain model](../domain/domain-model.md) | Accepted — [ADR-033](../adr/ADR-033-checkpoint-practice-workflow.md) |


## Deferred Decisions

| Topic | Current position | Re-evaluate when |
| --- | --- | --- |
| Authentication and multiple accounts | Design learner-owned data with future identity support; do not implement public accounts in the MVP. | A second real learner needs independent local/cloud data. |
| Cloud AI providers | Keep AI provider abstraction; use Ollama initially. | Local model quality, cost, or hosting requirements justify another provider. |
| Azure Blob Storage / cloud storage | Keep storage-provider abstraction; use local filesystem initially. | Shared/cloud deployment or multi-machine synchronization is needed. |
| Alternative vector database | Keep retrieval provider abstraction; use ChromaDB initially. | Scale, filtering, operational, or cloud-search needs justify migration. |
| Agent framework such as LangGraph | Keep product responsibilities modular; use custom orchestration initially. | Workflows require stateful graphs, checkpoints, complex branching, or human approval flows. |
| Mobile application | Keep backend APIs client-agnostic; do not build mobile clients now. | The web MVP is stable and real learner usage warrants it. |
| Public cloud deployment | Use Docker Compose locally; do not host publicly now. | The product needs access beyond the learner's local machine. |
| Branch protection on `main` | Rely on the pull-request workflow and CI checks; do not configure repository branch-protection rules yet. Branch protection is a repository setting, not a repository file, so it is not part of any change under version control. | A second contributor gains write access, or a merge bypasses CI in practice. See [ADR-010](../adr/ADR-010-feature-delivery-workflow.md). |
| Automated syllabus extraction | Keep curriculum data model generic; do not auto-create curricula from PDFs now. | GATE CSE workflow is stable and a reviewed setup experience is designed. |

## Accepted ADRs

These ADRs are accepted and hold the durable rationale, alternatives, and consequences for the decisions above:

| ADR | Decision | Register entry |
| --- | --- | --- |
| [ADR-001](../adr/ADR-001-clean-architecture.md) | Adopt Clean Architecture | DEC-005 |
| [ADR-002](../adr/ADR-002-provider-pattern.md) | Use provider interfaces for external capabilities | DEC-006 |
| [ADR-003](../adr/ADR-003-postgresql-persistence.md) | Use PostgreSQL for structured persistence | DEC-009 |
| [ADR-004](../adr/ADR-004-ollama-local-ai-provider.md) | Use Ollama as the initial local AI provider | DEC-011 |
| [ADR-005](../adr/ADR-005-docker-compose-local-development.md) | Use Docker Compose for local development | DEC-013 |
| [ADR-006](../adr/ADR-006-custom-agent-orchestration.md) | Start with a custom product-agent orchestrator | DEC-014 |
| [ADR-007](../adr/ADR-007-documentation-and-adr-policy.md) | Use repository documentation and ADRs as shared project memory | DEC-017 |
| [ADR-008](../adr/ADR-008-assessment-and-mistake-evidence-model.md) | Model assessment topics and mistake evidence sources explicitly | DEC-018, DEC-019, DEC-020 |
| [ADR-009](../adr/ADR-009-configuration-naming-and-validation.md) | Name and validate configuration variables explicitly | DEC-022 |
| [ADR-010](../adr/ADR-010-feature-delivery-workflow.md) | Deliver features through pull requests with automated gates | DEC-023 |
| [ADR-011](../adr/ADR-011-sqlalchemy-persistence-implementation.md) | Implement PostgreSQL persistence synchronously and migrate per milestone | DEC-024 |
| [ADR-012](../adr/ADR-012-curriculum-seed-and-reconciliation.md) | Load curriculum as reconciled reference data from a versioned file | DEC-003, DEC-004, DEC-025 |
| [ADR-013](../adr/ADR-013-examination-schedule-and-study-goal.md) | Model an examination period as a published window of reference data | DEC-026 |
| [ADR-014](../adr/ADR-014-api-response-contract.md) | Fix the public HTTP API response contract | DEC-027 |
| [ADR-015](../adr/ADR-015-frontend-foundation-and-server-rendered-api-access.md) | Build the frontend on Next.js and reach the API from the server | DEC-008 |
| [ADR-016](../adr/ADR-016-learner-onboarding-api-contracts.md) | Fix the learner setup API contracts | DEC-028 |
| [ADR-017](../adr/ADR-017-topic-progress-api-and-schema.md) | Record manual topic progress as a learner-owned stage | DEC-029 |
| [ADR-018](../adr/ADR-018-weekly-availability-slots.md) | Store weekly availability as named days replaced a week at a time | DEC-030 |
| [ADR-019](../adr/ADR-019-study-goal-planning-preferences.md) | Store planning preferences as typed columns replaced as a group | DEC-031 |
| [ADR-020](../adr/ADR-020-initial-study-plan-generation.md) | Generate the initial study plan deterministically as a roadmap and a week | DEC-032 |
| [ADR-021](../adr/ADR-021-plan-item-completion.md) | Mark a plan item completed as a reversible statement about work, not about the learner | DEC-033 |
| [ADR-022](../adr/ADR-022-plan-adaptation.md) | Adapt a study plan by rebuilding it around what happened | DEC-034 |
| [ADR-023](../adr/ADR-023-daily-study-view.md) | Show today's work as a reading of the weekly plan, not a daily plan | DEC-035 |
| [ADR-024](../adr/ADR-024-plan-item-skipping.md) | Let a learner skip a plan item, settling the item without retiring the topic | DEC-036 |
| [ADR-025](../adr/ADR-025-learner-postponement.md) | Let a learner postpone a plan item, settling it while the work waits for the next adaptation | DEC-037 |
| [ADR-026](../adr/ADR-026-monthly-study-view.md) | Show the month as a reading of the roadmap and the week, not a monthly plan | DEC-038 |
| [ADR-027](../adr/ADR-027-plan-feasibility.md) | Report whether the saved week reaches the horizon, as a read-only planning rule | DEC-039 |
| [ADR-028](../adr/ADR-028-revision-workflow.md) | Schedule revisions from finished work, on the learner's ask | DEC-040 |
| [ADR-029](../adr/ADR-029-progress-overview.md) | Show the progress overview as a reading of what is stored, counting nothing of its own | DEC-041 |
| [ADR-030](../adr/ADR-030-learning-stages-by-subject-panel.md) | Gather the recorded learning stages by subject, listing them rather than counting them | DEC-042 |
| [ADR-031](../adr/ADR-031-priority-focus-panel.md) | Draw priority focus from facts backend rules already decided, ranking nothing | DEC-043 |
| [ADR-032](../adr/ADR-032-learning-resource-catalogue.md) | Catalogue learner-owned study material as metadata, linked to topics | DEC-044 |
| [ADR-033](../adr/ADR-033-checkpoint-practice-workflow.md) | Assemble checkpoint practice from the learner's own questions, and report outcomes rather than a score | DEC-045 |

Decisions still marked **ADR pending** above have an approved direction but no formal ADR yet. Create the ADR before or alongside implementation of the affected area.

Additional ADRs should be added only for durable, consequential decisions. Avoid creating ADRs for routine file names, minor implementation details, or reversible experiments.

## Change Rule

When a decision in this register changes:

1. Update the detailed documentation that explains it.
2. Create a new ADR or mark the existing ADR as superseded when applicable.
3. Update this register with the new decision and links.
4. Do not silently change architecture through implementation alone.

## Related Documents

- [Project context](../00-project-context.md)
- [ADR directory](../adr/)
- [Architecture overview](overview.md)
- [Provider pattern](provider-pattern.md)
- [Technology stack](../development/tech-stack.md)
