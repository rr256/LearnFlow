# LearnFlow backend development and test dependencies.
#
# Includes everything in requirements.txt plus the tooling needed to run tests,
# lint, and format. Install with:
#
#   python -m pip install -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
httpx2==2.9.1
ruff==0.16.0
