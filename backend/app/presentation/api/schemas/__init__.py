"""Public request and response schemas for the LearnFlow API.

These are explicit contracts, separate from the application DTOs they are built
from and from the persistence models beneath those. No ORM model is ever
returned to a client (docs/api/conventions.md).
"""
