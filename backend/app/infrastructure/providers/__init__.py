"""Adapters for capability providers reached over a network.

Distinct from `persistence`, which adapts the database: everything here talks to
something outside the process. Today that is one AI provider, running locally.
"""
